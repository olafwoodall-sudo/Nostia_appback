# Dependencies - NEVER commit these
node_modules/
.expo/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Database - NEVER commit database files (contains user data)
database/*.db
database/*.db-shm
database/*.db-wal
database/backups/
*.sqlite
*.sqlite3

# Claude Code temporary files
tmpclaude-*
.claude/

# Large AI model files - NEVER commit (too large for git)
*.safetensors
*.bin
*.gguf
*.ggml
*.pt
*.pth
*.onnx
*.model

# DeepSeek AI model folder - NEVER commit (large model files)
deepseek-finetuned/
deepseek*/
ai-models/

# Logs and output files
*.output
nul
*.log

# Build outputs - NEVER commit these
dist/
build/
out/
target/
bin/
obj/
*.tgz
*.tar.gz
*.zip
*.exe
*.dll
*.so
*.dylib

# Environment - NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.example
.envrc
.env.template

# IDE files - NEVER commit these
.vscode/
.idea/
*.swp
*.swo
*.tmp
*.temp
*.bak
*.backup
*.orig
*.suo
*.ntvs*
*.njsproj
*.sln

# OS files - NEVER commit these
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
desktop.ini
Desktop.ini
$RECYCLE.BIN/

# Mobile specific - NEVER commit these
*.mobileprovision
*.keystore
*.jks
*.pem
*.p12
*.key
*.crt
*.csr

# Expo specific - NEVER commit these
.expo/
.expo-shared/

# React Native specific - NEVER commit these
ios/Pods/
ios/build/
android/.gradle/
android/build/
android/local.properties
nostia-android/local.properties
nostia-android/.gradle/
nostia-android/app/build/
*.xcworkspace
*.xcuserdata
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
*.xcuserstate
*.xcscmblueprint
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
*.moved-aside

# Package manager lock files (optional - uncomment if you don't want to commit)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml
